# VLA 与世界模型数据来源综述审阅记录

- 开始用户要求的一小时完善：2026-09-14 03:55:30 UTC（北京时间 11:55:30）。
- 最早完成时刻：2026-09-14 04:55:30 UTC。
- 文章：`content/posts/ai/vla-world-model-data/index.md`。
- 范围：VR/PICO、主从遥操作、UMI、第一视角与人类视频、真实机器人数据、仿真／生成数据，以及 VLA、动作条件世界模型与空间模型的监督区别。

## 已核对的关键事实

1. EgoHumanoid 的人体 RGB 来自外接 ZED X Mini；PICO 用于追踪。主体泛化实验的场景有人类数据覆盖。
2. EgoHumanoid v1 主体数字为平均阶段得分，正文约 31% → 82%，不是相对增加 51% 或整任务成功率。
3. EgoDex 829 小时包括约 725 小时训练、7 小时测试与 97 小时额外数据。
4. EgoScale 的大规模人体动作标签含 SLAM／手部估计与重定向；代码入口在核对时仍为 Coming Soon。
5. V-JEPA 2-AC 用相邻采样帧末端状态变化构造动作；无任务标注不等于没有状态／动作监督。
6. RoboCasa365 的 612 小时人工示范与 1,615 小时合成示范均在仿真中。
7. BEHAVIOR Challenge 2026 为 20,000 条／100 任务，2025 为 10,000 条／50 任务；joint_efforts 警告引用的是 2025 归档版本。
8. RoboMIND 2.0 的触觉与移动轨迹是主数据的子集；额外仿真轨迹另计。
9. AgiBot World 2026 Theme 3 官方 9 月说明报告 11,430 条；按主题与资源目录区分来源。
10. Atlas 为 2026-09-01 企业技术发布，部分伙伴早期访问；没有据其可接受输入推断完整训练语料。
11. 按具体作者核对 MimicPlay、BRS、BEHAVIOR、MoMaGen、SimFoundry 的 Li Fei-Fei 关联；未将 UMI、ALOHA、EgoScale、EgoHumanoid、RoboCasa 泛归到同一团队。

## 固定源码检查

- XRoboToolkit Python：`79e5cb8a56e3455515ce1b476e993c764ec58739`。
  - `DataLogger.add_entry` 不自动添加时间；调用方填写日志时间。
  - ARX 目标快照、控制发送与反馈属于不同读取／执行环节。
  - `timestamp_us` 字段直接保存 RealSense `get_timestamp()`，SDK v2.58.4 文档单位为 ms；使用者需核对转换层。
- UMI：`d095ba9590df789df5189eea5ee7e431689038a6`。
  - `relative` 为完整 SE(3) 相对变换；`rel` 保留旧实现。
  - 两种表示分别往返均可通过，混用产生方向错误；人工例子误差约 14.1 cm。
- 上述检查没有运行真实设备、策略训练或硬件性能实验。

## 教学实验与图示

- `data_contract_lab.py`：26 项检查，包含 400 个规则时间窗口。
- `source_probe.py`：读取固定 Git 对象执行日志与位姿转换函数，需 NumPy。
- imagegen 内置模式：一张六类来源概念图，提示词保存在 `docs/vla-world-model-data-image-prompts.json`。
- 三张原创 SVG：监督地图、空间资产到执行轨迹、观测可用时间。
- 所有人工数值和论文报告数值已在图注或正文中区分。

## 中途验证

- Hugo 草稿生产构建通过；最初发现的 SE(3) 公式被 Markdown 属性语法吞掉问题已修复。
- 全站验证：76 篇审阅、10 条学习路径、186 条自测、253 个草稿构建 HTML；没有错误或未渲染公式。
- 主页面 12 种宽度／主题组合、4 张图片加载、缩放、手机菜单及锚点通过。
- 分节检查 236 个布局（59 节 × 4 配置），表格可横向滚动，没有页面横溢出或 JS 异常。
- 第一轮外部链接验证 59 / 59 返回 200；新增来源须在发布前补检。

## 发布前验证

- 已完成草稿预览后设为正式文章；`hugo --minify --destination /tmp/vla-data-production-build` 通过，Hugo 0.165.0。
- 正式全站验证：76 篇审阅记录、73 篇已发布文章、10 条学习路径、186 条自测、243 个 HTML、17,051 个本地引用；代码片段与本文严格公式检查均无错误或警告。
- 文章分节检查：63 节 × 4 种宽度／主题，共 252 个布局；19 张表格中 18 张使用显式可读列宽。检查了末列可滚动到达、列宽下限与溢出表格的键盘聚焦。
- 根据 320 px 截图修正了源码表最后一列被挤成单字竖排的问题。可选 `table-readable` 类保持首列至少 7 rem，其余列至少 9 rem，使用现有横向滚动与主题配色。
- 正式文章主页面 12 种宽度／主题组合通过：31 个公式、4 张配图、图片缩放、手机目录与锚点均正常，没有 JS 异常。
- 博客索引在 320 / 1440 px、浅色／深色下输入 PICO 并筛选所属专题，均能找到新文章；学习路径入口可访问。
- 66 个正文外部链接与阅读索引中另外 12 个链接均返回 HTTP 200。可访问性检查不代表验证了远端所有权重、数据包或资源许可证。
- 实验包解压到独立临时目录，通过全部文件 SHA-256、26 项数据契约检查、固定源码探针结果比较；重新生成的三张 SVG 与包内文件逐字节一致。
- 补充了一个观测边界回归：`5.025 - 4` 的浮点结果不能让恰好到达的帧被当作未来帧。观测与动作时间均按声明的整数时间网格比较。
- 本地正式 HTTP 页面通过 canonical、13 个指定锚点、无草稿提示、下载文件内容一致及图片／响应式派生图访问检查。
- 生产部署沿用仓库现有的 main → GitHub Actions → GitHub Pages 流程；推送后的运行结论与线上核验结果在交付消息中报告。

## 线上核验与订阅入口

- 正文提交 `3d60641c0fe916675d6534bbca83a58caf4fb7ff` 的 [GitHub Actions 部署](https://github.com/chase6305/chase6305.github.io/actions/runs/34807202402)于 2026-09-14 04:46:43 UTC 完成，结论为 success。
- 线上核对了正文、9 个下载文件和 8 个图片资源（含头像及响应式派生图）；下载内容与工作区逐字节相同。
- 线上 390 px 深色与 1440 px 浅色视图通过，31 个公式和 4 张正文配图正常，缩放与目录可用。第一次检查遇到图片加载断言失败，重新加载复检通过；资源 HTTP 核对也通过。
- 订阅入口检查发现原主题把根节名 `posts` 当作文章类型过滤，而本站文章的类型是 `blog`，导致 `/posts/index.xml` 为空。
- 新增仓库内 `layouts/blog/list.rss.xml`，递归收录当前博客节的正式文章，输出摘要与正文链接，并遵守 Hugo RSS 数量配置。没有修改主题子模块。
- RSS 回归检查：正式构建收录 73 篇唯一文章，新文位于首项；带草稿构建仍只收录同样的 73 篇；所有生成 XML 均可解析，条目的标题、摘要、链接与 GUID 正常。

## 后续完善：监督接口与混合训练

- 补充 MimicPlay 的双相机标定与三维手轨迹监督，引用论文 Section 3.1–3.2；补充 EgoMimic 的共同相机参考系、共享位姿预测与机器人专用关节输出。
- 新增五种人类数据方法的训练接口对照，以及区分额外计算、人体监督和场景多样性的消融方案。
- 区分离线全序列估计制作标签与在线输入的信息可获得性，补充预训练语料覆盖范围的评测约束。
- 数据实验从 26 项增至 30 项：有效标签池均值 0.2、按来源等权均值 0.5、频次加权均值 0.5、不改变分母时 0.8；人工例子只解释归约规则，不报告真实训练收益。
- 重新生成实验包与哈希清单，并从独立解压目录复跑；数据实验、固定源码探针与三张 SVG 字节比较均通过。
- 正式构建及全站验证通过：73 篇正式文章、187 条自测、17,058 个本地引用，无公式或代码检查错误。
- 65 节 × 4 配置共 260 个分节布局通过；21 张表格中 20 张保持可读列宽，新增两节另做手机深色／桌面浅色截图检查。

## 后续完善：主手设计、格式边界与采集交付

- 增补 GELLO 的运动学主手设计与原论文测试平台，区分设计框架的适配能力、具体用户研究结论与力／触觉标签；引用原论文及作者项目页。
- 新增 RLDS / LeRobot v3 格式边界一节：最后观测与有效动作不同、环境终止与任务成功不同、多 episode 共享物理文件时需要通过元数据恢复轨迹范围。转换验收采用成功、超时和中断三类抽样。
- 新增试采交付与验收表，明确会话记录、控制字典、时间标定、轨迹索引和质量报告，并分别说明 PICO、主从、UMI 的证据要求；建议与官方格式定义分开标明。
- 阅读索引现为 37 项论文／项目，另列两项官方数据格式文档；同步 README、SHA256SUMS 与下载 ZIP。
- 新增仓库维护脚本 `scripts/package_robot_data_lab.py`。`--check --test` 只读核对归档和哈希，在独立解压目录比较 30 项实验结果、重建三张 SVG；可选的外部源码探针不在该命令中执行。
- 通过临时副本检验只读模式不改变文件、过期内容被拒绝、异常 ZIP 路径在解压前被拒绝。校验比较解压后内容，不依赖 zlib 的压缩字节一致性。
- Hugo 正式构建与全站验证通过：73 篇正式文章、187 条自测、17,066 个本地引用，无错误或警告。
- 新增章节完成手机深色与桌面浅色截图复核；67 节 × 4 配置共 268 个分节布局通过，22 张表格可滚动，其中 21 张保持可读列宽。最终仅调整阅读入口与第 4.1 节标题后重新构建、验证。

## 配图扩充与研究组织调整

- 按用户要求新增四张配图：遥操作双向链路、UMI 工具接口迁移、人体视频监督层次、动作条件下的不同后果；采用内置绘图工具，各自独立提示词记录在 `docs/vla-world-model-data-image-prompts.json`。
- 逐张检查人物／工具、运动与反馈方向、面板关系和配色后保存到文章 assets/；没有修改或覆盖原始图。四张新图均为 1536 × 1024，与原文的浅蓝、淡紫、木色和圆角分区保持一致。
- 正文现有八张图，均带连续图号、描述性替代文本与可见图注；新图另有对应的正文解读，图注只解释内容和证据范围，不标注制作工具名称。
- 第 9 节改为按采集、对齐、规划、环境、生成与预测的接口关系组织，删除围绕特定研究者身份的归类。新增 `research-connections` 锚点，并保留旧锚点兼容既有链接。
- 来源索引同步移除非必要的作者归类备注，重新打包实验文件及哈希；30 项实验与三张 SVG 重建检查通过。
- Hugo 正式构建及全站验证通过：73 篇正式文章、187 条自测、17,086 个本地引用，无错误或警告。
- HTML 核对八张图号、图注、替代文本和研究衔接入口；正文没有制作工具或特定研究者介绍文字。390 px 深色与 1440 px 浅色浏览器检查通过，八张图均正常加载响应式资源，图片放大、移动导航、35 个公式及内部链接正常；新图的手机和桌面截图已复核。

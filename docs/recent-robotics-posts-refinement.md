# 三篇机器人文章的补充审阅

日期：2026-09-12。基于此前固定的 X-VLA、RTC 与 Light-Loco-Parkour 源码版本，保留原论文报告与组件实现的区分。

## 本次补充

- X-VLA：基座／工具坐标与完整 SE(3) 增量的区别；相同轨迹中不同起始帧的未来标签重叠。两个 4 秒、30 标签窗口从 10/12 秒开始时，共享 15 个标签时间。
- RTC：会话标识先于请求顺序检查，防止任务重置后的旧结果安装；区分观测年龄与队列覆盖；固定延迟条件明确限定，波动时分别使用前次与本次延迟。H=8 时 2→5 可以覆盖，5→5 不可持续覆盖。
- Light-Loco-Parkour：实际组件的两环境全局重置对照；同一时间常数在 50/100 Hz 下的稳态幅值约 70.5545/130.2776。期望的逐环境重置是对照要求，不是现有上游功能。
- 三篇增加互相引用和分层阅读说明，同步阅读自测、实验说明、下载包及源码探针结果。

## 验证

- 两个实验 ZIP 重新生成，从临时解压目录执行其中 4 个程序通过。
- 标准库 parkour_lab.py 通过，teaching-results.json 同步；4 张 SVG 与生成器一致。
- 固定源码 CPU source_probe.py 通过，原有数值结果保持不变，新增 vectorized_reset 结果；标量日志显式 detach，避免梯度张量转换告警。
- Hugo 生产构建通过；237 个 HTML 页面、16,503 处本地引用检查无错误、未渲染公式或片段告警。
- 7 个新增／扩展章节分别在 390 像素深色和 1440 像素浅色下检查，共 14 组；没有溢出、公式错误或浏览器异常。
- 本地发布资源验证共 63 项，下载内容与工作区一致，包含更新的实验包和探针结果。

教学检查不代表模型性能、真实线程竞争或机器人闭环验证。本轮没有改动第三方仓库、主题、站点配置或部署工作流；沿用 main 分支的现有 GitHub Pages 部署。
